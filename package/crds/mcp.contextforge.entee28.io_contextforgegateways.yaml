---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: contextforgegateways.mcp.contextforge.entee28.io
spec:
  group: mcp.contextforge.entee28.io
  names:
    categories:
    - crossplane
    - managed
    - contextforge
    kind: ContextForgeGateway
    listKind: ContextForgeGatewayList
    plural: contextforgegateways
    singular: contextforgegateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Gateway is an example API type.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: A GatewaySpec defines the desired state of a Gateway.
            properties:
              forProvider:
                description: GatewayParameters are the configurable fields of a Gateway.
                properties:
                  auth:
                    properties:
                      basic:
                        properties:
                          secretName:
                            description: Secret must contain both a "username" and
                              "password" key
                            type: string
                        required:
                        - secretName
                        type: object
                      bearer:
                        properties:
                          secretRef:
                            description: |-
                              A LocalSecretKeySelector is a reference to a secret key
                              in the same namespace with the referencing object.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        required:
                        - secretRef
                        type: object
                      headers:
                        properties:
                          headers:
                            items:
                              properties:
                                name:
                                  type: string
                                secretRef:
                                  description: |-
                                    A LocalSecretKeySelector is a reference to a secret key
                                    in the same namespace with the referencing object.
                                  properties:
                                    key:
                                      type: string
                                    name:
                                      description: Name of the secret.
                                      type: string
                                  required:
                                  - key
                                  - name
                                  type: object
                              required:
                              - name
                              - secretRef
                              type: object
                            type: array
                        required:
                        - headers
                        type: object
                      oauth:
                        properties:
                          authorizationUrl:
                            type: string
                          clientId:
                            type: string
                          clientSecretRef:
                            description: |-
                              A LocalSecretKeySelector is a reference to a secret key
                              in the same namespace with the referencing object.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          grantType:
                            type: string
                          issuer:
                            type: string
                          jwksUri:
                            type: string
                          redirectUri:
                            type: string
                          scopes:
                            items:
                              type: string
                            type: array
                          tokenUrl:
                            type: string
                        required:
                        - clientId
                        - grantType
                        type: object
                      queryParam:
                        properties:
                          name:
                            type: string
                          secretRef:
                            description: |-
                              A LocalSecretKeySelector is a reference to a secret key
                              in the same namespace with the referencing object.
                            properties:
                              key:
                                type: string
                              name:
                                description: Name of the secret.
                                type: string
                            required:
                            - key
                            - name
                            type: object
                        required:
                        - name
                        - secretRef
                        type: object
                      type:
                        enum:
                        - basic
                        - bearer
                        - headers
                        - queryParam
                        - oauth
                        type: string
                    required:
                    - type
                    type: object
                    x-kubernetes-validations:
                    - message: basic is required when type is basic
                      rule: self.type != 'basic' || has(self.basic)
                    - message: bearer is required when type is bearer
                      rule: self.type != 'bearer' || has(self.bearer)
                    - message: headers is required when type is headers
                      rule: self.type != 'headers' || has(self.headers)
                    - message: queryParam is required when type is queryParam
                      rule: self.type != 'queryParam' || has(self.queryParam)
                    - message: oauth is required when type is oauth
                      rule: self.type != 'oauth' || has(self.oauth)
                  description:
                    type: string
                  name:
                    type: string
                  tags:
                    items:
                      type: string
                    type: array
                  teamRef:
                    description: TeamReference identifies a ContextForgeTeam resource
                      in the same namespace.
                    properties:
                      name:
                        type: string
                    required:
                    - name
                    type: object
                  transport:
                    enum:
                    - SSE
                    - STREAMABLEHTTP
                    type: string
                  url:
                    type: string
                  visibility:
                    description: |-
                      "private" is deliberately excluded. ContextForge scopes private
                      visibility to the creating identity's owner_email, but this provider
                      always authenticates as one shared service account - a private
                      resource would be invisible to the tenant who created it and their
                      team, not just restricted.
                    enum:
                    - team
                    - public
                    type: string
                required:
                - teamRef
                - transport
                - url
                - visibility
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: A GatewayStatus represents the observed state of a Gateway.
            properties:
              atProvider:
                description: GatewayObservation are the observable fields of a Gateway.
                properties:
                  createdAt:
                    type: string
                  enabled:
                    type: boolean
                  gatewayMode:
                    type: string
                  id:
                    type: string
                  lastError:
                    type: string
                  lastSeen:
                    type: string
                  name:
                    type: string
                  nextRetryAt:
                    type: string
                  reachable:
                    type: boolean
                  registrationAttempts:
                    type: integer
                  slug:
                    type: string
                  status:
                    type: string
                  statusMessage:
                    type: string
                  tags:
                    items:
                      type: string
                    type: array
                  teamId:
                    type: string
                  toolCount:
                    type: integer
                  transport:
                    type: string
                  updatedAt:
                    type: string
                  url:
                    type: string
                  visibility:
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
